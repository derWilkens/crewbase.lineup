<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://schemas.haulmont.com/cuba/screen/window.xsd"
        caption="msg://browseCaption"
        focusComponent="ticketsTable"
        messagesPack="eu.crewbase.lineup.web.ticket">
    <data readOnly="true">
        <!--collection id="ticketsDc"
                    class="eu.crewbase.lineup.entity.wayfare.Ticket"
                    view="ticket-view">
            <loader id="ticketsDl">
                <query>
                    <![CDATA[select e from lineup$Ticket e]]>
                </query>
            </loader>
        </collection-->
        <keyValueCollection id="ticketAmountDc">
            <loader id="ticketAmountDl">
                <query>SELECT c.startDate, t.startSite, t.destinationSite, COUNT(t) AS total, ct.seats as seat FROM lineup$Ticket t
                    LEFT JOIN t.transfer.crewChange c left join t.transfer.craftType ct
                    GROUP BY c.startDate, t.startSite, t.destinationSite
                    ORDER BY c.startDate, t.startSite.itemDesignation, t.destinationSite.itemDesignation ASC
                </query>
                <!--
                SELECT e FROM Employee e LEFT JOIN e.address a ORDER BY a.city
                -->
            </loader>
            <properties idProperty="total">
                <property name="startDate" datatype="dateTime"/>
                <property name="startSite" class="eu.crewbase.lineup.entity.coredata.Site"/>
                <property name="destinationSite" class="eu.crewbase.lineup.entity.coredata.Site"/>
                <property name="total" datatype="int"/>
                <property name="seat" datatype="int"/>
            </properties>
        </keyValueCollection>
    </data>
    <dialogMode height="600"
                width="800"/>
    <layout expand="ticketsTable"
            spacing="true">
        <!--filter id="filter"
                applyTo="ticketsTable"
                dataLoader="ticketAmountDl">
            <properties include=".*"/>
        </filter-->
        <table id="ticketsTable"
                    width="100%"
                    dataContainer="ticketAmountDc"
                    aggregatable="true">

            <columns>
                <column id="startDate"/>
                <column id="startSite"/>
                <column id="destinationSite"/>
                <column id="total"/>
                <column id="seat"/>
            </columns>
            <rowsCount/>
            <buttonsPanel id="buttonsPanel"
                          alwaysVisible="true">
            </buttonsPanel>
        </table>
        <hbox id="lookupActions" spacing="true" visible="false">
            <button action="lookupSelectAction"/>
            <button action="lookupCancelAction"/>
        </hbox>
    </layout>
</window>
